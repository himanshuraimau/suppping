{
  "name": "@suppping/worker",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@suppping/shared-types": "workspace:*",
    "bullmq": "^6.3.4",
    "ioredis": "^6.0.0",
    "pino": "^10.3.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.5.0",
    "typescript": "^7.0.2"
  }
}
